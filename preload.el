;; This is where you can place code that have to be executed before Adequate
;; starts loading modules.

;; Because this file is executed early, you can use it to alter the set of
;; modules that should be loaded. To do this, change the value of
;; `adq/module-load-order`.


